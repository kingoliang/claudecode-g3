---
description: OpenSpec 驱动的迭代式代码实现，结合规范管理和质量保证，支持跨会话恢复
argument-hint: [change-id]
---

# OpenSpec 迭代式实现

变更 ID: $ARGUMENTS

---

## ⚠️ MANDATORY EXECUTION RULES (必须严格遵守)

**以下规则必须严格执行，不得跳过或简化。违反任何规则视为执行失败。**

### Rule 1: Agent 调用 (MUST)

你**必须**使用 Task tool 调用以下 agents，**禁止**直接编写代码或跳过检查：

```
代码生成:    Task(subagent_type="code-writer", prompt="...")
安全检查:    Task(subagent_type="security-reviewer", prompt="...")
质量检查:    Task(subagent_type="quality-checker", prompt="...")
性能分析:    Task(subagent_type="performance-analyzer", prompt="...")
结果聚合:    Task(subagent_type="result-aggregator", prompt="...")
```

### Rule 2: 迭代循环 (MUST)

对每个任务，你**必须**执行以下循环，直到 PASS 或达到最大迭代次数(5轮)：

```
iteration = 0
feedback = ""

WHILE (NOT PASS AND iteration < 5):
    iteration++

    # Step A: 你必须调用 code-writer
    result = Task(
        subagent_type="code-writer",
        prompt="任务: {task_description}\n规范: {spec}\n设计: {design}\n反馈: {feedback}"
    )

    # Step B: 你必须在【单个消息】中【并行】调用 3 个 reviewer
    # 发送一条消息，包含 3 个 Task 调用
    security_result = Task(subagent_type="security-reviewer", ...)
    quality_result = Task(subagent_type="quality-checker", ...)
    performance_result = Task(subagent_type="performance-analyzer", ...)

    # Step C: 你必须调用 result-aggregator 汇总
    aggregated = Task(
        subagent_type="result-aggregator",
        prompt="汇总以下检查结果:\n安全: {security_result}\n质量: {quality_result}\n性能: {performance_result}"
    )

    # Step D: 根据结果决定下一步
    IF aggregated.result == "PASS":
        更新 tasks.md 标记完成
        进入下一个任务
        BREAK
    ELSE IF aggregated.result == "ITERATE":
        feedback = aggregated.feedback_for_code_writer
        继续循环
    ELSE IF aggregated.result == "STALLED":
        记录阻塞问题
        请求人工介入
        BREAK
```

### Rule 3: 禁止行为 (FORBIDDEN)

- ❌ **禁止**: 直接编写代码而不调用 code-writer agent
- ❌ **禁止**: 跳过任何 reviewer agent 的检查
- ❌ **禁止**: 不调用 result-aggregator 就判定是否通过
- ❌ **禁止**: 逐个调用 reviewer agents (必须并行，单个消息 3 个 Task)
- ❌ **禁止**: 修改代码后不重新运行检查流程

### Rule 4: context.md 生成 (MUST)

会话结束前，你**必须**生成或更新 `openspec/changes/$ARGUMENTS/context.md` 文件，包含：

```markdown
## Session {YYYY-MM-DD HH:MM}

- **Completed Tasks**: [列出完成的任务]
- **Validation Scores**: Security: X/100, Quality: X/100, Performance: X/100
- **Issues Encountered**: [列出遇到的问题]

## Quick Resume (For Context Reset)

> **TL;DR**: [一句话总结当前状态]

**Last Working State**:
- **Current Task**: [当前任务]
- **Files Modified**: [修改的文件列表]
- **Compilation Status**: [编译状态]
- **Blockers**: [阻塞问题]

**Next Steps**:
1. [下一步操作 1]
2. [下一步操作 2]
```

---

## 完整工作流程

```
┌─────────────────────────────────────────────────────────────┐
│                    OpenSpec 生命周期                         │
│  /os-proposal → /os-apply-iterative → /os-archive           │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│  Step 1: 加载 OpenSpec 文档                                  │
│  - proposal.md (目标和范围)                                  │
│  - design.md (技术设计)                                      │
│  - tasks.md (任务清单)                                       │
│  - context.md (恢复上次会话)                                 │
│  - specs/*.md (需求规范)                                     │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│  Step 2: 恢复会话上下文                                      │
│  - 读取 context.md 的 "Quick Resume"                        │
│  - 确定从哪个任务继续                                        │
│  - 加载已知阻塞问题                                          │
└─────────────────────────────────────────────────────────────┘
                           ↓
            ╔═══════════════════════════════════╗
            ║  Step 3: 对每个待完成任务执行      ║
            ║         迭代式协作循环             ║
            ╚═══════════════════════════════════╝
                           ↓
┌─────────────────────────────────────────────────────────────┐
│  3.1 code-writer 编写代码                                    │
│  - 输入: 任务描述 + spec.md + design.md                      │
│  - 输出: 代码实现                                            │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│  3.2 并行质量检查                                            │
├───────────────┬───────────────┬─────────────────────────────┤
│ security      │ quality       │ performance                 │
│ -reviewer     │ -checker      │ -analyzer                   │
└───────────────┴───────────────┴─────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│  3.3 result-aggregator 汇总判定                              │
│  - 达标 → 进入下一任务                                       │
│  - 未达标 → 反馈给 code-writer，继续迭代                     │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│  Step 4: 更新 OpenSpec 文档                                  │
│  - context.md (会话历史、进度)                               │
│  - tasks.md (任务状态)                                       │
└─────────────────────────────────────────────────────────────┘
```

## 详细执行步骤

### Step 1: 加载 OpenSpec 文档

读取 `openspec/changes/$ARGUMENTS/` 目录下的所有文档:

```bash
# 必需文件
openspec/changes/$ARGUMENTS/proposal.md    # 理解变更目标
openspec/changes/$ARGUMENTS/design.md      # 理解技术设计
openspec/changes/$ARGUMENTS/tasks.md       # 获取任务清单
openspec/changes/$ARGUMENTS/context.md     # 恢复会话状态

# 规范文件
openspec/changes/$ARGUMENTS/specs/*/spec.md  # 需求规范
```

### Step 2: 恢复会话上下文

从 `context.md` 的 **Quick Resume** 部分获取:

1. **上次工作状态**
   - 正在处理的任务
   - 已修改的文件
   - 编译/测试状态

2. **待完成任务**
   - 从 tasks.md 获取未完成任务
   - 按照原有顺序执行

3. **已知阻塞问题**
   - 需要处理的技术债务
   - 需要人工介入的问题

### Step 3: 对每个任务执行迭代式协作

对 `tasks.md` 中每个状态为 `⏳ Not Started` 或 `🏗️ In Progress` 的任务:

#### 3.1 初始代码生成

使用 `code-writer` Agent:

```json
{
  "task": "当前任务描述",
  "spec": "来自 spec.md 的需求",
  "design": "来自 design.md 的技术设计",
  "context": "项目上下文和约束"
}
```

#### 3.2 并行质量检查

**同时启动 3 个检查 Agent:**

| Agent | 检查内容 | 达标标准 |
|-------|---------|---------|
| security-reviewer | 安全漏洞 | ≥ 85 分, 无 Critical |
| quality-checker | 代码质量 | ≥ 80 分 |
| performance-analyzer | 性能问题 | ≥ 80 分 |

#### 3.3 结果聚合与判定

使用 `result-aggregator` Agent:

- **PASS**: 更新 tasks.md，进入下一任务
- **ITERATE**: 反馈给 code-writer，继续迭代 (最多 5 轮)
- **STALLED**: 记录阻塞，请求人工介入

### Step 4: 更新 OpenSpec 文档

#### 更新 context.md

每次任务完成后，追加会话记录:

```markdown
## Session YYYY-MM-DD HH:MM

- **Duration**: X hours
- **Completed Tasks**:
  - Task 1.1: 创建 UserController ✅
  - Task 1.2: 创建 UserService ✅
- **Key Decisions**:
  - 使用 BCrypt 进行密码哈希
  - JWT 过期时间设为 1 小时
- **Code Changes**:
  - Added: UserController.java (120 lines)
  - Added: UserService.java (85 lines)
  - Modified: SecurityConfig.java (+15 lines)
- **Issues Encountered**: 无
- **Validation Results**:
  - Security: 92/100
  - Quality: 88/100
  - Performance: 95/100

## Quick Resume (For Context Reset)

> **TL;DR for next session**: 完成了用户认证基础功能，下次继续实现 JWT 刷新

**Last Working State**:
- **What I was doing**: Task 2 - JWT 刷新逻辑
- **Files modified**:
  - UserController.java (完成)
  - UserService.java (完成)
  - TokenService.java (进行中)
- **Compilation status**: ✅ Clean
- **Test status**: ✅ 12/12 pass
- **Blockers**: 无

**Next Steps**:
1. 实现 TokenService.refreshToken() 方法
2. 添加刷新令牌存储逻辑
3. 编写刷新令牌测试
```

#### 更新 tasks.md

```markdown
### Phase 1: Foundation & Setup

- [x] Task 1.1: 创建 UserController ✅
  - **Completed**: 2025-01-15 14:30
  - **Iterations**: 2
  - **Final Score**: 91/100

- [x] Task 1.2: 创建 UserService ✅
  - **Completed**: 2025-01-15 15:45
  - **Iterations**: 1
  - **Final Score**: 94/100

- [ ] Task 1.3: 创建 TokenService 🏗️
  - **Started**: 2025-01-15 16:00
  - **Current Iteration**: 1
```

## 迭代控制

| 参数 | 值 | 说明 |
|------|-----|------|
| 每任务最大迭代 | 5 轮 | 防止无限循环 |
| 达标综合评分 | ≥ 80 | 加权平均 |
| 安全评分阈值 | ≥ 85 | 安全优先 |
| 停滞检测 | 2 轮 | 连续进步 < 5 分 |

## 输出

### 成功完成所有任务
```
✅ OpenSpec 变更实现完成！

## 变更: $ARGUMENTS
- 任务完成: 5/5 (100%)
- 总迭代次数: 8
- 最终综合评分: 89/100

## 下一步
运行 `/openspec:archive $ARGUMENTS` 归档此变更
```

### 部分完成 (会话结束)
```
🔄 会话保存成功

## 进度
- 任务完成: 3/5 (60%)
- 当前任务: Task 4 - Integration tests

## 恢复方式
下次运行 `/os-apply-iterative $ARGUMENTS` 将自动从 Task 4 继续

## 保存的文件
- context.md (已更新)
- tasks.md (已更新)
```

## 使用示例

```bash
# 1. 首先创建提案
/openspec:proposal "实现用户认证系统"

# 2. 使用迭代式协作实现
/os-apply-iterative CHG-user-auth

# 3. 下次会话继续 (自动恢复)
/os-apply-iterative CHG-user-auth

# 4. 完成后归档
/openspec:archive CHG-user-auth
```

## 与现有 OpenSpec 命令的关系

| 命令 | 用途 | 替代/增强 |
|------|------|----------|
| `/openspec:proposal` | 创建变更规范 | 保持不变 |
| `/openspec:apply` | 原有的实现命令 | **此命令增强** |
| `/os-apply-iterative` | 迭代式实现 (带质量保证) | 新增 |
| `/openspec:archive` | 归档变更 | 保持不变 |

**推荐**: 对于需要高质量保证的功能，使用 `/os-apply-iterative` 替代 `/openspec:apply`。
